# Theme previews

Store one preview image per theme in this directory.

Use the same lowercase kebab-case base name as the theme configuration:

```text
themes/amethyst-night.toml
assets/previews/amethyst-night.png
```

PNG and WebP are recommended. Crop screenshots to the terminal area and check that they do not expose private paths, usernames, hostnames, tokens, or command history.
